import { AppLayout } from "@/components/layouts/app-layout"

export default AppLayout

export const metadata = {
  title: "Dashboard | Sistema de Gestión",
  description: "Panel de control del sistema de gestión de tours",
}
